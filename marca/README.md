# Marca — arquivos originais

Os logotipos como o cliente entregou. Esta pasta é o **arquivo morto**: nada aqui é
servido pela aplicação.

O que a aplicação usa está em `imigrar-agent/public/marca/`, já recortado (inclusive o
símbolo isolado, em 256px e em branco) e com nome em kebab-case. Se um logotipo mudar,
atualize aqui **e** lá — e leia `imigrar-agent/IDENTIDADE.md` antes, porque a paleta do
painel é amostrada pixel a pixel de `logotipo-original.png`.

`nao-identificado-design-goworking.jpg` veio junto no mesmo download do Drive e não tem
relação aparente com a Imigrar Brasil. Ficou aqui em vez de ser apagado, com o nome
dizendo o que é. Se ninguém reclamar dele, pode sair.
